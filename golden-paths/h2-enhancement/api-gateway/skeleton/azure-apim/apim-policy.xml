<!--
  Azure API Management Policy for ${{ values.gatewayName }}
  Generated by Open Horizons Golden Path
-->
<policies>
  <inbound>
    <base />

    <!-- Rate Limiting -->
    <rate-limit-by-key
      calls="${{ values.rateLimits }}"
      renewal-period="60"
      counter-key="@(context.Subscription?.Key ?? context.Request.IpAddress)"
      increment-condition="@(context.Response.StatusCode >= 200 && context.Response.StatusCode < 400)" />

    <!-- CORS -->
    <cors allow-credentials="false">
      <allowed-origins>
        <origin>*</origin>
      </allowed-origins>
      <allowed-methods preflight-result-max-age="300">
        <method>GET</method>
        <method>POST</method>
        <method>PUT</method>
        <method>DELETE</method>
        <method>PATCH</method>
        <method>OPTIONS</method>
      </allowed-methods>
      <allowed-headers>
        <header>Content-Type</header>
        <header>Authorization</header>
        <header>X-Request-ID</header>
      </allowed-headers>
    </cors>

    <!-- JWT Validation -->
    <validate-jwt header-name="Authorization" failed-validation-httpcode="401" require-scheme="Bearer">
      <openid-config url="https://login.microsoftonline.com/common/v2.0/.well-known/openid-configuration" />
      <required-claims>
        <claim name="aud" match="any">
          <value>api://${{ values.gatewayName }}</value>
        </claim>
      </required-claims>
    </validate-jwt>

    <!-- Request ID propagation -->
    <set-header name="X-Request-ID" exists-action="skip">
      <value>@(Guid.NewGuid().ToString())</value>
    </set-header>
  </inbound>

  <backend>
    <base />
  </backend>

  <outbound>
    <base />
    <set-header name="X-Gateway" exists-action="override">
      <value>${{ values.gatewayName }}</value>
    </set-header>
  </outbound>

  <on-error>
    <base />
    <set-header name="X-Error-Source" exists-action="override">
      <value>apim-policy</value>
    </set-header>
  </on-error>
</policies>
